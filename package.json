{
  "name": "multi-arena",
  "version": "8.2.1",
  "private": true,
  "description": "Multi Arena: veintiún juegos competitivos en una sola plataforma",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@types/node": "^22.15.0",
    "socket.io": "4.8.3",
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "prestart": "npm run build",
    "start": "npm --workspace backend start",
    "test": "npm --workspace backend test"
  }
}

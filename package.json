{
  "name": "sudoku-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Sudoku Arena multiplayer prototype",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm --workspace backend run build",
    "prestart": "npm run build",
    "start": "npm --workspace backend start",
    "test": "npm --workspace backend test"
  }
}
